<div class="custom-card" id="custom-card-<%= product.id %>">

  <div class="custom-card-top">
    <div class="cct-absolute-quotation-container" id="form-steps-<%= product.id %>"></div>

    <%= render "products/product_services", product: product %>

    <div class="cct-title">
      <%= product.title %>
      <div class="cct-copyright">®</div>
    </div>

    <% if product.powers.length != 0 %>
      <div class="cct-powers">
        <% active_powers(product.powers).each do |tag| %>
          <%= tag %>
        <% end %>
      </div>
    <% end %>

    <div class="cct-guarantee" data-id="<%= product.id %>">
      <div class="services-guarantee">services</div>
      <div class="icon-setting-tools" aria-hidden="true" style="margin-right: 12px;font-size: 20px;"></div>
    </div>
  </div>


  <div class="custom-card-middle">
    <div class="ccm-left">
      <% infos_constructor(product.infos, "ccm-info-container", "ccm-info-content").each do |info_tag| %>
        <%= info_tag %>
      <% end %>
    </div>
    <div class="ccm-right" style="background-image: url(<%= cl_image_path product.photos.last.path  %>);"></div>
  </div>



  <div class="custom-card-bottom">
    <%= render 'scores/form', category: product.category, product: product, score: @score %>

    <div class="ccb-middle">
      <%= link_to new_category_product_quotation_path(product.category, product) do %>
        <div>devis gratuit</div>
      <% end %>
    </div>

    <div class="separator-v2"></div>

    <div class="ccb-right">
      <%= link_to "#productShowModalv2", class: "custom-card-bottom-right-btn", data: { toggle: "modal", typeid: "#{product.category.id}", productid: "#{product.id}" } do %>
        <div>photos</div>
      <% end %>
    </div>

    <div class="separator-v2"></div>



      <div class="custom-card-bottom-right-absolute">
        <div id="score-count-<%= product.id %>">
          <div class="score-count-number"><%= product.scores.length %> avis</div>
        </div>

        <div class="average-score-graph" id="score-graph-<%= product.id %>">
          <div class="average-dyn-data" style="background-color: <%= score_color(average_score(product.scores)) %>;width: <%= (average_score(product.scores) * 10).to_i %>%;"></div>
        </div>

        <div id="score-text-<%= product.id %>"><div class="score-text"><%= average_score(product.scores) %></div></div>

        <div class="separator-v3"></div>

        <div class="score-form-trigger" id="score-form-trigger-<%= product.id %>" data-id="<%= product.id %>" style="display: flex;">
          <div class="icon-like" aria-hidden="true" style="padding-top: 4px;font-size: 15px;"></div>
        </div>
      </div>


  </div>

</div>




